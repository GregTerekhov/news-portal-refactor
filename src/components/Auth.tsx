import React from 'react';
import { PrimaryButton } from 'ui';

const Auth = () => {
  return (
    <div>
      <PrimaryButton type='button' name={'Signin/Signup'} />
    </div>
  );
};

export default Auth;
